import { Modal, StyleSheet } from 'react-native'

interface Props {
  open: boolean
  close: () => void
}

const MenuModal = ({open , close} : Props) => {
  return (
    <Modal >
        
    </Modal>
  )
}
export default MenuModal
const styles = StyleSheet.create({})